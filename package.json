{
  "name": "macro-intelligence-terminal",
  "version": "1.0.0",
  "description": "Next-generation macroeconomic intelligence platform",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
